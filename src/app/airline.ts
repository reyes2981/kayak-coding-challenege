export interface Airline {
    id: number;
    site: string,
    code: string,
    alliance: string,
    phone: string,
    name: string,
    usName: null,
    _clazz: string,
    defaultName: null;
    logoURL: string;
}